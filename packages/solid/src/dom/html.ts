import { createHTML } from "lit-dom-expressions";
import {
  effect,
  style,
  insert,
  createComponent,
  delegateEvents,
  classList,
  dynamicProperty,
  setAttribute,
  setAttributeNS,
  Attributes,
  SVGAttributes,
  NonComposedEvents,
  SVGElements,
  SVGNamespace
} from "./index.js";

export default createHTML({
  effect,
  style,
  insert,
  createComponent,
  delegateEvents,
  classList,
  dynamicProperty,
  setAttribute,
  setAttributeNS,
  Attributes,
  SVGAttributes,
  NonComposedEvents,
  SVGElements,
  SVGNamespace
});
