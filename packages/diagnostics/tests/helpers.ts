/**
 * Wall-clock attribution warnings are valuable in real captures but depend on
 * host load. Tests assert structural diagnostics and rerun counts instead.
 */
export const deterministicAttribution = { hotTime: false } as const;
