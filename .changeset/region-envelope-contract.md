---
"@solidjs/signals": patch
"@solidjs/babel-plugin-jsx": patch
"@solidjs/compiler": patch
"solid-js": patch
"@solidjs/web": patch
---

Region ENVELOPE CONTRACT (compiler audit response): compiled scopes split into a source-ordered compute envelope (every user expression evaluates in the pure phase — eligible chains on the pending-aware raw with `_d$` step resolution, safe residuals rewritten, opaque expressions and `prop:` sinks tracked through the proxy) and an effect-phase commit that only compares and writes (force flag for first-run writes, baselines advance after each write). Demotion rebinds under the mounting owner with its first commit deferred to the effect phase; deletions demote (overlay raw cannot represent them); admission requires plain prototypes; `region` is exported under the server condition. Both compilers emit the contract byte-identically; snapshots are the anchor.
