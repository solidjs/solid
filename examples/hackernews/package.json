{
  "name": "hackernews-example",
  "description": "Solid Server Components over frame streams: the same HN app as ../hackernews-spa — same routes, same markup, same data layer — with the static markup returned by server components, so it arrives as HTML once and never again as hydration data",
  "version": "0.0.0",
  "private": true,
  "author": "Ryan Carniato",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@solidjs/router": "2.0.0-next.13",
    "@solidjs/web": "workspace:*",
    "solid-js": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "vite-plugin-solid": "3.0.0-next.22"
  }
}
