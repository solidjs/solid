---
"@solidjs/signals": patch
---

Remove the unsupported single-argument `createEffect` overload so TypeScript requires a separate effect callback while preserving the development runtime diagnostic.
