import { createHyperScript } from "hyper-dom-expressions";
import { spread, assign, insert, createComponent, dynamicProperty } from "./index.js";

export default createHyperScript({
  spread,
  assign,
  insert,
  createComponent,
  dynamicProperty
});
