{
  "name": "migrating-element-example",
  "description": "Side-by-side demo of Solid 2.0 DOM-node migration: a single canvas is reused across multiple JSX slots and preserves its bitmap, animation state, and native event listener through every move.",
  "version": "0.0.0",
  "private": true,
  "author": "Ryan Carniato",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "start": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@solidjs/vite-plugin": "3.0.0-next.33",
    "@solidjs/web": "workspace:*",
    "@types/node": "^25.6.0",
    "solid-js": "workspace:*",
    "typescript": "^6.0.2",
    "vite": "^7.0.0"
  }
}
