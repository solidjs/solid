---
"solid-js": patch
---

A `"recovery"` record on `OBSERVE.records` (`RecoveryEvent`): the client rendering a `<Loading>` boundary the server handed over — its fragment rejected or the stream was cut — with how long the fallback stood (`waitedMs`) and what the fresh render cost (`renderMs`), joined to the server's `"boundary"` record by `id`.
