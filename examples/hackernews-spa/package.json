{
  "name": "hackernews-spa-example",
  "description": "The SSR-SPA comparison baseline for the hackernews server-components example: the SAME app — same routes, same components, same data — but server functions return JSON and client components render everything, with the standard hydration data blob",
  "version": "0.0.0",
  "private": true,
  "author": "Ryan Carniato",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js"
  },
  "dependencies": {
    "@solidjs/router": "1.0.0-next.11",
    "@solidjs/web": "workspace:*",
    "solid-js": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "vite-plugin-solid": "3.0.0-next.17"
  }
}
