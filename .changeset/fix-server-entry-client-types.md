---
"@solidjs/web": patch
---

Type the server entry against the client `solid-js` declarations it actually resolves, type the client `HydrationScript` stub as a JSX component, and point published `@solidjs/web` types at the same JSX module `jsx-runtime` uses so router `Action`s type-check as form `action`s.
