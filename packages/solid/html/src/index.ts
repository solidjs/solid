import { createHTML } from "lit-dom-expressions";
import {
  effect,
  style,
  insert,
  createComponent,
  delegateEvents,
  classList,
  dynamicProperty,
  setAttribute,
  setAttributeNS,
  Aliases,
  Properties,
  ChildProperties,
  NonComposedEvents,
  SVGElements,
  SVGNamespace
} from "solid-js/web";

export default createHTML({
  effect,
  style,
  insert,
  createComponent,
  delegateEvents,
  classList,
  dynamicProperty,
  setAttribute,
  setAttributeNS,
  Aliases,
  Properties,
  ChildProperties,
  NonComposedEvents,
  SVGElements,
  SVGNamespace
});
